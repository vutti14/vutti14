# RABBiZBuild Mini ERP — ระบบเบิกจ่ายงานก่อสร้าง

ระบบจัดการงานก่อสร้างที่สร้างจากสองไฟล์ต้นทาง:

| ไฟล์ต้นทาง | ใช้ทำอะไร |
|---|---|
| `docs/SPEC_RABBiZBuild_v1.md` | สเปกเวอร์ชัน V1 (เฟส 1 + เฟส 2) — โครงสร้าง กฎ สิทธิ์ หน้าจอ |
| `data/source/RABBiZBuild_v8_FINAL.xlsx` | ฐานข้อมูล 32 ชีต — โครงการ อาคาร ผู้ขาย วัสดุ BOQ และรายการเบิก 1,449 รายการ |

ข้อมูลย้อนหลังถูกนำเข้าครบ **1,449 ใบ · 15,153,287.91 บาท** ตรงกับยอดในไฟล์ต้นทางทุกสตางค์

---

## เริ่มใช้งานใน 3 คำสั่ง

```bash
npm install        # ต้องมี Node.js 20.11 ขึ้นไป
npm run setup      # สร้างฐานข้อมูล + นำเข้าข้อมูลจากไฟล์ v8
npm start          # เปิด http://localhost:3000
```

เปิดด้วยเบราว์เซอร์บนมือถือได้ทันที (หน้าจอออกแบบมาให้ใช้มือเดียว)

**เข้าสู่ระบบครั้งแรก:** username = อีเมลในชีต `05_DIM_PERSON` · รหัสผ่าน = เบอร์โทรของคนนั้น
ระบบจะบังคับเปลี่ยนรหัสผ่านทันทีที่ล็อกอินครั้งแรก และบังคับตั้ง 2FA สำหรับ CEO / COO / การเงิน

| คน | บทบาท | อีเมล | รหัสผ่านครั้งแรก |
|---|---|---|---|
| พี่อาร์ต | CEO | rabbizgroup001@gmail.com | 0924242626 |
| กร | COO | rabbizgroup004@gmail.com | 0964040444 |
| ตั้ม | การเงิน | rabbizgroup003@gmail.com | 0625599000 |
| พี่หน่อย | บัญชี | rabbizgroup002@gmail.com | 0819595500 |
| แม็ก · เบน · พี่เอฟ · คุณหนึ่ง | PM | rabbizgroup011/012/010/013@gmail.com | เบอร์โทรของแต่ละคน |
| เจ | ช่างซ่อมบำรุง | rabbizgroup014@gmail.com | 0969360980 |
| เต้ · อิม | ช่างซ่อมบำรุง · ผู้ดูข้อมูล | *ยังไม่มีในฐาน v8* | บัญชีถูกระงับไว้ รอ CEO กรอกอีเมลจริงในหน้าตั้งค่า |

คำสั่งอื่น:

```bash
npm run reset      # ล้างฐานข้อมูลแล้วนำเข้าใหม่ทั้งหมด
npm test           # ทดสอบกฎและเส้นทางงานทั้งระบบ (20 ชุด)
npm run seed:xlsx  # แปลง .xlsx ต้นทางเป็น data/seed/*.json ใหม่ (ต้องมี python3 + openpyxl)
```

ตั้งค่าเพิ่มเติมผ่านตัวแปรสภาพแวดล้อม: `PORT` · `RABBIZ_DB` · `RABBIZ_UPLOAD_DIR`

---

## หน้าจอ

| หน้า | ใคร | ทำอะไร |
|---|---|---|
| **ภาพรวม** | ทุกคน | เงินออกเดือนนี้ · รออนุมัติ · จ่ายแล้วรอเอกสาร · **ของที่จ่ายแล้วยังไม่มา** · 11 ตารางวิเคราะห์ |
| **เบิกเงิน** | PM · ช่างซ่อมบำรุง · COO · CEO | สร้างใบเบิกด้วยมือเดียว — ปุ่มใหญ่ 4 ปุ่มสำหรับประเภทต้นทุน จำค่าล่าสุดที่ใช้ สร้างผู้ขายใหม่ได้ในหน้าเดียวกัน |
| **อนุมัติ** | COO · CEO | ใบไม่มีธง = เลือกหลายใบกดครั้งเดียว · ใบมีธง = ต้องเปิดดูและกดทีละใบ |
| **จ่ายเงิน** | การเงิน | คิวใบที่อนุมัติแล้ว · แนบสลิปบังคับ · เตือนเครดิตค้างกับผู้ขาย |
| **เอกสาร** | บัญชี | เอกสารค้างเรียงตามยอดเงิน · ติ๊กทีละช่อง · แยกวันที่บนเอกสารจากวันที่ได้รับ · สรุปภาษีซื้อรายเดือนภาษี |
| **ใบเบิก** | ทุกคน | ค้นหา กรอง ดูรายละเอียด ประวัติการแก้ไข |
| **ผู้ขาย** | ทุกคน | สร้าง แก้ไข ยืนยันเอกสาร (คนสร้างกับคนยืนยันต้องคนละคน) |
| **การเงิน** | การเงิน · บัญชี · CEO | เงินทุนเข้า · เครดิตค้าง · ใบกลับรายการ · เงินสดย่อย |
| **ตั้งค่า** | CEO | ผู้ใช้และสิทธิ์ · ผัง Expense Code · สวิตช์กฎเตือน W2 · ส่งออกข้อมูล · สถานะข้อมูล |

---

## กฎที่ระบบบังคับให้

### กฎบล็อก — ผ่านไม่ได้

| รหัส | กฎ |
|---|---|
| B1 · B2 | ทุกบรรทัดต้องมีหมวดงานและประเภทต้นทุน |
| B3 · B4 | ต้องเลือกโครงการ อาคาร ผู้ขาย และระบุว่ามี VAT หรือไม่ |
| B5 | ผลรวมรายการย่อยต้องเท่ากับยอดหัวใบ และจำนวน × ราคาต้องเท่ากับจำนวนเงิน |
| B6 | ใบเกิน 5,000 บาท ต้องแนบไฟล์อย่างน้อย 1 ไฟล์ก่อนส่งขออนุมัติ |
| B7 | บันทึกการจ่ายต้องแนบสลิป |
| B8 | หมวดงานที่เลิกใช้/ยุบรวมไม่ปรากฏใน dropdown และเลือกไม่ได้ |
| B9 | ยอดเงินของใบที่จ่ายแล้วแก้ไม่ได้ตลอดกาล (แก้ได้เฉพาะอาคารและหมายเหตุ พร้อมเหตุผลลง `audit_log`) |
| B10 | เงินสดย่อยต่อรายการไม่เกิน 2,000 บาท และต้องเคลียร์บิลก่อนเติมเงิน |

### กฎเตือน — ผ่านได้แต่ติดธง (ผู้อนุมัติต้องกดทีละใบ)

W1 ผู้ขายรายใหม่ยังไม่เคยจ่าย · W2 ราคาสูงกว่าอ้างอิงเกิน 10% *(ปิดสวิตช์ไว้)* ·
W3 ผู้ขายค้างเอกสารเกิน 3 ใบ · W4 มีเครดิตค้างกับผู้ขายรายนี้ ·
W5 รายการซ้ำ (วันเดียวกัน **อาคารเดียวกัน** ยอดตรงกัน) · W6 ยอดซ้ำภายใน 7 วัน ·
W7 ใบของผู้อนุมัติเอง · W8 เกินงบอาคาร · W9 ยอดลงท้าย 000 และเกิน 20,000

> **W2 ถูกปิดสวิตช์ไว้ตามสเปก §12** — ฟีเจอร์สร้างเสร็จแล้วแต่ยังไม่เปิด เพราะยังจับคู่วัสดุไม่ครบ
> เปิดได้ที่ **ตั้งค่า → สวิตช์ระบบ** เมื่อจับคู่วัสดุ 6 คู่ที่ค้างอยู่เสร็จ

### กฎอัตโนมัติ

- VAT คำนวณตามวิธีที่เลือก (แยก: ×7% · รวมแล้ว: ÷1.07) และเก็บทั้งยอดก่อน VAT / VAT / ยอดรวมเสมอ
- หัก ณ ที่จ่าย = %ของผู้ขาย × **ยอดบรรทัดที่เป็นค่าแรงหรือค่าเช่าเท่านั้น** คิดจากฐานก่อน VAT
- รหัสอ้างอิงเดิม 10 หลักสร้างเอง (ผู้เบิก 1 + โครงการ 3 + อาคาร 3 + หมวดงาน 3) — คนพิมพ์เองไม่ได้
- วันเบิกใบแรก/ใบล่าสุด/ระยะเวลาของอาคาร คำนวณจากใบเบิกจริง ไม่ให้คนกรอก
- อายุเอกสารนับจากวันจ่าย — 15 วันเหลือง · 30 วันแดง
- ใบปิดรายการอัตโนมัติเมื่อ จ่ายแล้ว + ของมาครบ + เอกสารครบตามประเภท

---

## หลักการออกแบบ 10 ข้อ ทำที่ไหนในโค้ด

| หลักการ | ทำที่ |
|---|---|
| 1. ห้ามพิมพ์รหัสเอง | ทุกช่องรหัสเป็น `<select>` (`public/js/views/request-form.js`) |
| 2. ห้ามลบ ห้ามแก้ยอดที่จ่ายแล้ว | `FROZEN_STATUSES` ใน `server/rules.js` + ใบกลับรายการ `reversals` |
| 3. รหัสสั้น ไม่แบกความหมาย | `building_id` เป็น B001–B072 · คุณสมบัติเก็บเป็นคอลัมน์ |
| 4. ทุกค่าต้องรู้ที่มา | คอลัมน์ `value_source` ในทุกตารางหลัก + `confidence` A/B/C/D |
| 5. เพิ่มแกน ไม่เพิ่มโค้ด | `cost_code` (แกน 1) × `cost_type` (แกน 2) แทนการแตกรหัสใหม่ |
| 6. เทียบต้นทุนเฉพาะ design เดียวกัน | `GET /api/reports/cost-per-sqm` จัดกลุ่มตาม `design_code` เท่านั้น |
| 7. วันเริ่มสร้าง = วันเบิกใบแรก | คำนวณด้วย `MIN(request_date)` ไม่มีช่องให้กรอก |
| 8. แสดงเป็นยอดเงิน | หน้าเอกสารและ Dashboard เรียงตามบาท ไม่ใช่จำนวนใบ |
| 9. ตรวจซ้ำที่ระดับอาคาร | `evaluateFlags` W5/W6 จับคู่ด้วย `building_id` ไม่ใช่ `project_id` |
| 10. มือถือมาก่อน | CSS มือถือมาก่อน ปุ่มสูง 48px ปุ่มประเภทต้นทุน 52px |

---

## ข้อมูลย้อนหลัง แยกจากข้อมูลใหม่เสมอ

รายการทั้ง 1,449 ใบถูกนำเข้าเป็นสถานะ **จ่ายแล้ว** และตั้ง `value_source = 'นำเข้าย้อนหลัง'`

- กรองแยกได้ทุกหน้าจอ (ตัวกรอง "ที่มาของค่า")
- ในรายการใบเบิก ใบนำเข้ามีแถบเทาด้านซ้าย
- ระดับความเชื่อถือ **C** (672 ใบ · 7.63 ล้านบาท) และ **D** แสดงด้วยสีต่าง พร้อมคำว่า "ประมาณ"
  เพราะการแยกค่าของ/ค่าแรงในสองระดับนี้เป็นการอนุมานจากสัดส่วน BOQ ไม่ใช่ข้อเท็จจริง
- **เอกสารไม่ถูกนำเข้า** ตามสเปก §9 — หน้าตามเอกสารและตัวเลข "ของที่จ่ายแล้วยังไม่มา"
  จึงนับเฉพาะใบที่เกิดในระบบ ไม่รวมข้อมูลนำเข้า
- รายการที่ไม่มีรหัสอาคารในไฟล์ต้นทาง (194 ใบ) ถูกพักไว้ที่อาคารชื่อ **"ไม่ระบุอาคาร"** ของแต่ละโครงการ
  (`is_building = N` จึงไม่เข้าตัวหารต้นทุนต่อ ตร.ม.) — ย้ายเข้าอาคารจริงได้จากปุ่ม "แก้ข้อมูลประกอบ"

ส่งออกกลับเป็น CSV (UTF-8 + BOM เปิดใน Excel ได้เลย) ที่ **ตั้งค่า → ส่งออกข้อมูล**
ครอบคลุม 15 ตาราง รวมใบเบิก รายการย่อย การจ่าย เอกสาร และประวัติการแก้ไข

---

## โครงสร้างโปรเจกต์

```
server/
  schema.sql        โครงสร้างฐานข้อมูล 28 ตาราง
  db.js             การเชื่อมต่อ · audit log · เลขที่เอกสาร
  auth.js           bcrypt · TOTP 2FA (เขียนเอง ไม่พึ่งไลบรารีภายนอก) · สิทธิ์ตามบทบาท
  rules.js          กฎ B1–B10 · ธง W1–W9 · VAT · หัก ณ ที่จ่าย · เอกสารที่ต้องครบ
  setup.js          สร้างตาราง + นำเข้าข้อมูลตั้งต้น
  routes/           auth · master · requests · payments · documents · finance · reports · admin
public/
  index.html        เปลือกแอป
  css/app.css       สไตล์เดียวทั้งระบบ มือถือมาก่อน
  js/app.js         router · สถานะรวม · หน้าเข้าสู่ระบบ
  js/views/         หน้าจอทั้ง 9 หน้า
tools/
  xlsx_to_seed.py   แปลง .xlsx เป็น JSON (รันครั้งเดียว ผลลัพธ์ commit ไว้แล้ว)
data/
  source/           ไฟล์ Excel ต้นทาง v8
  seed/             ข้อมูลตั้งต้นรูปแบบ JSON
tests/
  api.test.js       ทดสอบกฎและเส้นทางงานทั้งระบบ
  ui-smoke.mjs      ทดสอบหน้าจอบนมือถือจำลอง (ต้องติดตั้ง playwright เพิ่ม)
```

ไม่มีขั้นตอน build — หน้าเว็บเป็น ES module ล้วน เปลี่ยนไฟล์แล้วรีเฟรชได้เลย

---

## สิ่งที่ยังไม่ได้ทำ และเหตุผล

**ตามสเปก §11 (ตั้งใจไม่ทำใน V1)** — BOQ generator อัตโนมัติ · ฝั่งรายรับ/ค่าเช่า · ROI ต่ออาคาร ·
รายงานภาษีอัตโนมัติ (ระบบเตรียมข้อมูลให้ บัญชียื่นเอง) · ระบบใบสั่งซื้อ (PO) และการจับคู่ 3 ทาง · แอปติดตั้ง

**ต้องต่อบริการภายนอกก่อนถึงใช้ได้**

- **อนุมัติผ่านไลน์ (สเปก §8 S2)** — ระบบมีช่องเก็บ `line_user_id` และ API ผูกบัญชีให้แล้ว
  (`POST /api/auth/line/link`) แต่การส่งการ์ดเข้าไลน์ต้องมี LINE Messaging API channel
  (channel secret + access token) ซึ่งยังไม่มี จึงยังส่งการ์ดไม่ได้ — อนุมัติผ่านเว็บบนมือถือได้ตามปกติ
- **2FA** ใช้แอป Authenticator มาตรฐาน (Google Authenticator, Authy) โดยพิมพ์รหัสลับหรือกดลิงก์ `otpauth://`
  ยังไม่มีรูป QR ให้สแกน เพราะจะต้องเพิ่มไลบรารีสร้าง QR

**จุดที่ทำต่างจากสเปกเล็กน้อย พร้อมเหตุผล**

1. **การเงิน (ตั้ม) สร้างใบเบิกเองไม่ได้** — สเปก §3.1 ระบุสิทธิ์ของ FINANCE ไว้แค่
   บันทึกการจ่าย/แนบสลิป/บันทึกรับเงินคืน แต่ข้อมูลย้อนหลังพบว่าตั้มเคยเบิกเอง 17 รายการ 14,106 บาท
   ระบบทำตามสเปกไว้ก่อน ถ้าต้องการเปิดสิทธิ์ ให้เพิ่ม `'request.create'` ในบทบาท `FINANCE`
   ที่ `server/auth.js` (มีคอมเมนต์กำกับไว้แล้ว)
2. **ประเภทต้นทุนมี 4 ปุ่ม ไม่ใช่ 3** — สเปก §5.1 กำหนดค่าคงที่ 4 ค่า (ของ · แรง · เช่า · โสหุ้ย)
   แต่ภาพหน้าจอ §8 วาดไว้ 3 ปุ่ม ระบบใช้ 4 ปุ่มตามโครงสร้างข้อมูล
   (ค่าน้ำมัน ค่าส่งของ ค่าเดินทาง ค่าสาธารณูปโภคในข้อมูลจริงเป็นโสหุ้ย ไม่มีปุ่มให้เลือกไม่ได้)
3. **มีประเภทต้นทุนที่ห้าคือ "ไม่ระบุ"** ใช้ได้เฉพาะข้อมูลนำเข้าย้อนหลัง 995,193 บาท
   ที่ไฟล์ v8 ยังแยกไม่ได้ (ระดับ D) — เลือกไม่ได้ในใบใหม่ และไม่อยู่ใน dropdown
4. **รหัส `NA` (ไม่ระบุ) ถูกตั้งเป็น "เลิกใช้"** เพราะไม่ใช่หมวดงานจริง ทำให้เหลือหมวดงานที่ใช้ต่อ 29 รหัสพอดีตามสเปก §5.1
5. **รหัส `TRK`** ที่สเปก §5.1 ระบุว่าให้เลิกใช้ ไม่มีอยู่ในไฟล์ v8 จึงไม่ได้สร้างขึ้นมา

**งานที่ยังค้างฝั่งข้อมูล (สเปก §12 — ไม่บล็อกการใช้ระบบ)**

| งาน | ทำที่ไหนในระบบ |
|---|---|
| กรอกพื้นที่ ตร.ม. 12 อาคาร | หน้าใบเบิก → เปิดอาคาร หรือ `PATCH /api/buildings/:id` |
| ตั้ง design_code ให้ครบ 55 อาคาร | เหมือนกัน |
| ยืนยันอัตราค่าแรง 8 อัตรา | ตาราง `rates` สถานะ "รอยืนยัน" |
| จับคู่วัสดุ 6 คู่ แล้วเปิด W2 | ตั้งค่า → สวิตช์ระบบ |
| ตรวจรายการซ้ำ 12 กลุ่ม | ภาพรวม → แท็บ "รายการซ้ำ" |

---

## ตัวเลขวัดความสำเร็จ

ภาพรวม → แท็บ **"การใช้ระบบ"** แสดงตัวเดียวที่สเปก §10 กำหนดไว้:

> **% ของใบเบิกที่ผ่านระบบ ไม่ใช่ผ่านไลน์**
> เดือนแรก 60% · เดือนสาม 95% = สำเร็จ
> เดือนสามยัง 40% = ระบบใช้ยากเกินไป ต้องแก้ที่ระบบ ไม่ใช่ตำหนิคน

นับจากวันตัดข้อมูล **1 กันยายน 2569** (แก้ได้ที่ ตั้งค่า → สวิตช์ระบบ)
