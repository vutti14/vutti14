# ปรับปรุงเว็บ UX / UI / SEO — 2 เว็บ

| เว็บ | ธุรกิจ | ตรวจเมื่อ | เอกสาร |
|---|---|---|---|
| **www.หินอ่อน.com** | SIAM STONE — หินอ่อน หินแกรนิต | 3 ก.ย. 2026 | [`docs/AUDIT.md`](docs/AUDIT.md) |
| **www.silalang.com** | ศิลาเพชร — เหมืองศิลาแลงกำแพงเพชร | 4 ก.ย. 2026 | [`docs/AUDIT-silalang.md`](docs/AUDIT-silalang.md) |

ทั้งสองเว็บอยู่บน **LnwShop** เหมือนกัน จึงมีปัญหาหลายข้อร่วมกันและแก้ด้วยวิธีเดียวกันได้

**ข้อค้นพบที่ใช้ข้ามเว็บได้:** silalang.com มี **GA4 (`G-FPKE24QWKN`) ทำงานอยู่จริง**
ผ่าน GTM container ในขณะที่หินอ่อน.com ไม่มีเลย — เอาวิธีตั้งค่าเดียวกันไปใช้ได้ทันที
ไม่ต้องเริ่มจากศูนย์

---

# ส่วนที่ 1 — www.หินอ่อน.com (SIAM STONE)

ตรวจและทดสอบกับเว็บจริงเมื่อ 3 กันยายน 2026

> **เว็บนี้อยู่บน LnwShop** ซึ่งเป็นแพลตฟอร์มสำเร็จรูป ไม่ใช่โค้ดที่เราแก้แล้ว deploy ได้
> ทุกอย่างในรีโปนี้จึงเป็น **โค้ดและเนื้อหาที่ก๊อปไปวางในหลังร้าน** ไม่ใช่ pull request
> ทุกชิ้นทดสอบกับหน้าเว็บจริงแล้วว่าใช้ได้ — ดูผลก่อน/หลังใน [`docs/AUDIT.md`](docs/AUDIT.md)

---

## เริ่มตรงไหน

อ่าน [`docs/AUDIT.md`](docs/AUDIT.md) ก่อน — มีสรุปว่าอะไรพัง อะไรดีอยู่แล้ว
และคำถามเชิงกลยุทธ์ที่ควรตัดสินใจก่อนลงแรงเยอะ

---

## ลำดับการลงมือ (เรียงตามผลตอบแทนต่อเวลาที่ใช้)

### 🔴 วันแรก — 30 นาที ผลใหญ่ที่สุด

| # | ทำอะไร | ไฟล์ | ที่ไหน |
|---|---|---|---|
| 1 | ติด **GA4** | `patch/head-snippet.html` ส่วน [3] | ตั้งค่าร้านค้า → โค้ดเสริม → `<head>` |
| 2 | วาง **JS เสริม** (หยุดโหลดรูปที่มองไม่เห็น ~5 MB) | `patch/custom.js` | แต่งร้าน → โค้ดเสริมท้ายหน้า |
| 3 | วาง **CSS เสริม** (แก้หน้าเลื่อนออกข้าง + ปุ่มเล็ก) | `patch/custom.css` | แต่งร้าน → CSS เสริม |
| 4 | วาง **Schema ธุรกิจ** | `patch/head-snippet.html` ส่วน [1][2] | ตั้งค่าร้านค้า → โค้ดเสริม → `<head>` |

> ⚠️ ก่อนวาง `head-snippet.html` แก้ 2 จุด: `G-XXXXXXXXXX` (ใส่ GA4 ID จริง)
> และตรวจรหัสไปรษณีย์กับเวลาทำการ

### 🟠 สัปดาห์แรก — งานเนื้อหา

| # | ทำอะไร | ไฟล์ |
|---|---|---|
| 5 | เพิ่ม **H1 + ปุ่มโทร/LINE** ในหน้าแรก | `content/home-hero.html` |
| 6 | เปลี่ยน **ตารางราคาจากรูป → เป็นข้อความ** | `content/price-table.html` |
| 7 | อัปโหลด **รูปที่บีบอัดแล้ว** ทับของเดิม | `assets/optimized/` |
| 8 | แก้ **title / description** ทั้ง 12 หมวด | `content/meta-rewrites.csv` |
| 9 | ย่อ **ส่วนต่อท้าย title** เหลือ `หินอ่อน.com` | ตั้งค่าร้านค้า → SEO |
| 10 | ใส่ **`alt` รูปสินค้า 55 ชิ้น** (เขียนไว้ให้ครบแล้ว) | `content/alt-text.csv` |
| 11 | เปิดหน้า **FAQ** พร้อม FAQPage schema | `content/faq-page.html` |
| 12 | เปลี่ยน **คำอธิบายสินค้าทั้ง 55 ชิ้น** (เขียนใหม่ให้ครบแล้ว) | `content/product-descriptions.md` |
| 13 | อัปโหลด **รูปใหม่ 20 ใบ** สร้างจากรูปสินค้าจริง | `assets/generated/` |

### 🟡 เดือนแรก — งานที่ต้องใช้เวลา

| # | ทำอะไร | ไฟล์ |
|---|---|---|
| 14 | ตัด **tag page** ตามคำตัดสินรายหน้า (95 → เก็บ 40 ตัด 55) | `content/tag-audit.csv` |
| 15 | เชื่อม **บทความ 27 ชิ้น → หน้าสินค้า** ตามตารางจับคู่ | `content/article-product-links.csv` |
| 16 | เปิด **หน้าผลงาน** + เริ่มถ่ายรูปงานตามคู่มือในไฟล์ | `content/portfolio-page.html` |
| 17 | แก้บทความ 2 ชิ้นที่ไม่มีสินค้ารองรับ + พิจารณาสั่งของเข้า | `content/stock-gap-analysis.md` |
| 18 | ยืนยัน **Google Business Profile** + ทำ NAP ให้ตรงกัน | `content/content-plan.md` |

---

## ตรวจว่าได้ผลจริงไหม

```bash
npm i -g playwright        # ครั้งแรกครั้งเดียว
node tools/audit-live.mjs  # วัดเว็บจริงตอนนี้
```

รันก่อนลงงาน เก็บผลไว้ แล้วรันซ้ำหลังลง — จะเห็นตัวเลขขยับจริง ไม่ต้องเชื่อใคร

ดูล่วงหน้าว่า patch จะเปลี่ยนอะไรบ้าง โดยยังไม่ต้องแตะเว็บจริง:

```bash
node tools/audit-live.mjs --patch
```

---

## แผนผังไฟล์

```
docs/AUDIT.md                 ผลตรวจเต็ม + ผลทดสอบก่อน/หลัง + คำถามเชิงกลยุทธ์
patch/head-snippet.html       Schema.org + GA4 + preconnect      → วางใน <head>
patch/custom.css              แก้ layout, ปุ่มเล็ก, ฟอนต์มือถือ    → วางใน CSS เสริม
patch/custom.js               lazy-load รูป, H1 สำรอง             → วางท้าย <body>
content/home-hero.html        บล็อก H1 + ปุ่มโทร/LINE หน้าแรก
content/price-table.html      ตารางราคาแบบข้อความ (แทนรูป 3 MB)
content/meta-rewrites.csv     title/description ใหม่ 12 หมวด + หน้าแรก
content/alt-text.csv          alt ภาษาไทยของสินค้าครบ 55 ชิ้น (เขียนจากการดูรูปจริงทุกใบ)
content/tag-audit.csv         คำตัดสิน เก็บ/ตัด ครบทั้ง 95 หน้า tag พร้อมจำนวนสินค้าจริง
content/faq-page.html         หน้า FAQ 13 คำถาม + FAQPage schema 9 คำถาม
content/article-product-links.csv  ตารางจับคู่บทความ 27 ชิ้น → หน้าสินค้า/หมวดหมู่
content/product-descriptions.md   คำอธิบายสินค้าใหม่ 55 ชิ้น (เฉลี่ย 582 ตัวอักษร จากเดิม ~100)
content/product-descriptions.csv  เวอร์ชันตาราง เทียบของเดิมกับของใหม่
content/portfolio-page.html   หน้าผลงาน + คู่มือถ่ายรูปงานสำหรับช่างและเซลล์
content/stock-gap-analysis.md หินที่มีบทความแต่ไม่มีขาย + ช่องว่างในสต๊อก
content/content-plan.md       แผนเนื้อหา ลิงก์ภายใน และ Local SEO
assets/optimized/             รูปบีบอัดแล้ว พร้อมอัปโหลดทับ (−80%)
assets/generated/             รูปใหม่ 20 ใบ สร้างจากรูปสินค้าจริง + alt ครบทุกใบ
tools/audit-live.mjs          สคริปต์วัดเว็บจริง ใช้เทียบก่อน/หลัง
tools/crawl-site.sh           นับสินค้าในหน้า tag ทั้งหมด ใช้ตรวจซ้ำหลังตัด tag
tools/make-boards.mjs         สร้างรูป 20 ใบใหม่เมื่อราคาเปลี่ยน
```

---

## สิ่งที่ชุดนี้ **ไม่ได้** แก้ให้

พูดตรง ๆ เพื่อไม่ให้คาดหวังผิด:

- **jQuery 1.12.4 (ปี 2016), ไฟล์ Flash `.swf`, CSS กว้างคงที่ 1200px** — เป็นของ LnwShop แก้ไม่ได้
- **`": Inspired by LnwShop.com (v2)"` ท้าย title ทุกหน้า** — ต้องถาม LnwShop ว่าปลดที่แพ็กเกจไหน
- **อันดับปัจจุบันใน Google** — ต้องเปิดสิทธิ์ Search Console ให้ก่อนถึงจะดูได้
- **รูปสินค้าที่สร้างด้วย AI** — ทำไม่ได้ในเซสชันนี้ และไม่แนะนำแม้ทำได้
  หินธรรมชาติไม่มีสองแผ่นที่ลายเหมือนกัน รูปที่ AI สร้างจึงเป็นการโฆษณาเกินจริง
  เหตุผลเต็มอยู่ใน `assets/generated/README.md` — แทนที่ด้วยรูปใหม่ 20 ใบจากของจริง
- **รูป "ก่อน" สำหรับหน้าผลงาน** — ไม่พบรูปก่อนงานเลยบนเว็บปัจจุบัน
  ทำโครงหน้าและคู่มือถ่ายรูปไว้ให้แล้วใน `content/portfolio-page.html`
- **`alt` ของรูปที่สอง–สี่ในแต่ละสินค้า และรูปในบทความ** — ทำรูปหลักครบ 55 ชิ้นแล้ว
  ที่เหลือทำต่อได้ถ้าต้องการ แต่รูปหลักคือตัวที่ขึ้น Google Images ให้ผลคุ้มที่สุดก่อน
- **ปุ่มที่ยังเล็กอยู่ 15–20 จุดต่อหน้า** — จาก 64–101 จุด เหลือ 15–20
  ที่เหลือฝังอยู่ใน component ของ LnwShop ที่ขยายแล้วเสี่ยงทำ layout เพี้ยน

> รอบแรกผมเขียนไว้ว่า `alt` และปุ่มเล็ก "แก้ไม่ได้" — ทั้งสองข้อผิด แก้แล้วทั้งคู่
> รายละเอียดอยู่ในหัวข้อ "แก้ไขข้อสรุปเดิม 2 ข้อ" ของ `docs/AUDIT.md`


---

# ส่วนที่ 2 — www.silalang.com (ศิลาเพชร)

ตรวจและทดสอบกับเว็บจริงเมื่อ 4 กันยายน 2026 · แพลตฟอร์ม LnwShop เหมือนกัน

**อ่านก่อน:** [`docs/AUDIT-silalang.md`](docs/AUDIT-silalang.md)

## สรุปสั้น

เว็บนี้ **ทำหลายอย่างดีกว่าหินอ่อน.com** — มี `<h1>` ในหน้าแรก, GA4 ทำงานจริง,
Facebook Pixel ติดอยู่, บทความ 19 ชิ้นตรงคำถามคนซื้อ, ไม่มี horizontal scroll

แต่มีปัญหาที่หนักกว่าอยู่สองข้อ:

| ปัญหา | ตัวเลข |
|---|---|
| **หน้าแรกโหลดรูป 14.38 MB** ทุกไฟล์เป็น `_raw` ไม่ผ่านการย่อ | โปสเตอร์ 5 ใบเป็น **PNG** ขนาด 1.0–1.6 MB ต่อใบ |
| **เนื้อหาขายของทั้งหมดฝังอยู่ในรูป** | หน้าแรกมีข้อความจริงแค่ 2,916 ตัวอักษร (หินอ่อน.com 5,789) |
| ไม่มีรูปไหน lazy-load | 27/27 · 39/39 · 24/24 |
| 18 จาก 24 รูปไม่มี alt ที่ใช้งานได้ | ไม่มี alt 7 · alt ว่าง 11 |
| **คำโฆษณาพิสูจน์ไม่ได้** | หมวด `/category/15/` ชื่อ "หญ้าเทียม ราคาถูกที่สุดในโลก" |

## ผลทดสอบก่อน/หลัง (แทรก patch เข้าเว็บจริงแล้ววัดซ้ำ)

| หน้า | รูปที่ไม่ lazy | ปุ่มเล็กเกิน 44px (มือถือ) |
|---|---|---|
| หน้าแรก | 27/27 → **2/27** | 34/54 → **14/54** |
| หมวดศิลาแลงเหลี่ยม | 16/16 → **2/16** | 49/70 → **16/70** |
| หมวดพระบูชา (39 สินค้า) | 39/39 → **2/39** | 74/118 → **16/118** |
| หน้าสินค้า | 22/22 → **2/22** | 40/60 → **16/60** |
| หน้าบทความ | 24/24 → **2/24** | 56/77 → **13/87** |

เดสก์ท็อป: horizontal overflow 2px → 0 ทุกหน้า

## ลำดับการลงมือ

| # | ทำอะไร | ไฟล์ |
|---|---|---|
| 1 | วาง **JS เสริม** — lazy-load รูป 14 MB | `patch-silalang/custom.js` |
| 2 | วาง **CSS เสริม** — ขยายปุ่มมือถือ, แก้ overflow | `patch-silalang/custom.css` |
| 3 | อัปโหลด **รูปที่บีบอัดแล้ว** ทับของเดิม (−81%) | `assets/silalang-optimized/` |
| 4 | **แก้คำ "ถูกที่สุดในโลก"** ในชื่อหมวด — เสี่ยงผิดกฎหมาย | `content-silalang/claims-review.md` |
| 5 | เพิ่ม **ข้อความจริงในหน้าแรก** (ถอดจากโปสเตอร์) | `content-silalang/home-hero.html` |
| 6 | วาง **Schema ธุรกิจเหมือง** | `patch-silalang/head-snippet.html` |
| 7 | แก้ **title / description** 8 หน้า | `content-silalang/meta-rewrites.csv` |
| 8 | **ลบ UA tag ที่ตายแล้ว 2 ตัว** ออกจาก GTM | ดูหมายเหตุท้าย `head-snippet.html` |
| 9 | ใส่ **alt ให้รูปโปสเตอร์** | ตัวอย่างใน `assets/silalang-optimized/README.md` |

## แผนผังไฟล์ (silalang)

```
docs/AUDIT-silalang.md            ผลตรวจเต็ม + ผลทดสอบก่อน/หลัง + สิ่งที่ทำดีกว่าหินอ่อน.com
patch-silalang/head-snippet.html  Schema เหมืองศิลาแลง + รายการ tag ที่ควรลบ (ไม่ต้องเพิ่ม GA4)
patch-silalang/custom.css         ขยายปุ่มมือถือ, กันรูปล้น, สไตล์ hero และ figcaption
patch-silalang/custom.js          lazy-load, fetchpriority, เติม alt, ย่อรูป _raw ที่เป็นจัตุรัส
content-silalang/home-hero.html   ข้อความจริงที่ถอดจากโปสเตอร์ + วิธีทำ figcaption
content-silalang/claims-review.md คำโฆษณาที่พิสูจน์ไม่ได้ 2 จุด พร้อมข้อความทดแทน
content-silalang/meta-rewrites.csv title/description ใหม่ 8 หน้า
assets/silalang-optimized/        รูปบีบอัดแล้ว 7 ไฟล์ ลด 81-85% + ตัวอย่าง alt
```

## ตรวจสอบเอง

```bash
npm i -g playwright
# แก้ BASE ใน tools/audit-live.mjs เป็น https://www.silalang.com
# และแก้ path ของ patch เป็น patch-silalang/
node tools/audit-live.mjs
node tools/audit-live.mjs --patch
```

## ต้องแก้ก่อนเอาไปใช้จริง (silalang)

- `patch-silalang/head-snippet.html` — กรอกที่อยู่เหมือง พิกัด เวลาทำการ
- `content-silalang/home-hero.html` — กรอกค่าขนส่ง ขั้นต่ำ ระยะเวลาส่ง และเรื่องใบกำกับภาษี
- `patch-silalang/custom.js` บล็อก `[D]` — ถ้าอัปโหลดรูปใหม่แล้ว ให้ลบบล็อกนี้ทิ้ง
- **"บ่อศิลาแลงที่ใหญ่ที่สุดในภาคเหนือ"** บนโปสเตอร์ — เตรียมหลักฐานรองรับ
  หรือเปลี่ยนเป็น "เหมืองศิลาแลงของเราเอง" ซึ่งจริงและแรงกว่า
